{
  "name": "test-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^7.1.5",
    "@babel/core": "^7.22.9",
    "jest": "^29.6.2",
    "typescript": "^5.1.6"
  }
}